schema:
  version: 1

event:
  name: register:cpf:daily:changes
  version: 1

types:

validation:
  payload:
    created:
      of: date(yyyy-MM-dd HH:mm:ss)
      is:
        - required
  identity:
    userIds:
      of: array(long)
      is:
        - required
